# Legacy Configs

Old-format xtr_estimator YAML files kept for reference.
These reference pre-processed MTZ files under `initial/` (now `data/`) that may no longer exist.
Use `configs/datasets/` for all current runs.
